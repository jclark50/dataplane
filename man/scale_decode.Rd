% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{scale_decode}
\alias{scale_decode}
\title{Decode scaled integer columns back to floats (using audit metadata)}
\usage{
scale_decode(dt, audit_dt, keep_storage = TRUE, overwrite = FALSE)
}
\arguments{
\item{dt}{A `data.frame` or `data.table`.}

\item{audit_dt}{Parsed audit/plan table. Must contain:
`column`, `writer_scale`, `storage_column`, `did_scale`.}

\item{keep_storage}{Logical. If `TRUE`, keep the integer storage columns.}

\item{overwrite}{Logical. If `TRUE`, overwrite existing base columns if present.}
}
\value{
A decoded `data.table` (modified copy).
}
\description{
If your writer created columns like `temp_i` with `writer_scale = 100`,
this recreates `temp = temp_i / 100`. Optionally keeps or drops the
integer storage columns.
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(temp_i = c(2512L, 2544L))
audit <- data.table(column="temp", writer_scale=100, storage_column="temp_i", did_scale=TRUE)
scale_decode(dt, audit)
}
}
