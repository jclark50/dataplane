% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_delta.R
\name{dp_write_parquet_delta}
\alias{dp_write_parquet_delta}
\title{Write a Parquet file with explicit lossless numeric encodings}
\usage{
dp_write_parquet_delta(
  x,
  path,
  python = NULL,
  compression_level = 6L,
  row_group_size = 1000000L,
  validate_read = FALSE
)
}
\arguments{
\item{x}{A `data.frame`, `data.table`, or Arrow `Table`.}

\item{path}{Output `.parquet` path.}

\item{python}{Optional Python executable; see [dp_delta_check()].}

\item{compression_level}{ZSTD compression level from 1 through 22.}

\item{row_group_size}{Positive number of rows per Parquet row group.}

\item{validate_read}{If `TRUE`, read the completed temporary Parquet in R and
compare every value and null with the input Arrow table.}
}
\value{
Invisibly returns write metadata including path, size, row count, and timing.
}
\description{
Integer columns use `DELTA_BINARY_PACKED`; floating-point columns use
`BYTE_STREAM_SPLIT`; character and dictionary columns retain dictionary
encoding. The function stages Arrow IPC, validates the Parquet metadata, and
atomically replaces the destination only after a successful write.
}
