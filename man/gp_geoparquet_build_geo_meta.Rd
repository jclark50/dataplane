% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_geo.R
\name{gp_geoparquet_build_geo_meta}
\alias{gp_geoparquet_build_geo_meta}
\title{Build a GeoParquet `geo` metadata object}
\usage{
gp_geoparquet_build_geo_meta(
  geometry_columns,
  primary_column = geometry_columns[1],
  encoding = "WKB",
  geometry_types = NULL,
  crs = NULL,
  edges = c("planar", "spherical"),
  orientation = c(NULL, "counterclockwise"),
  bbox = NULL,
  covering_bbox = NULL,
  version = "1.1.0",
  extra = list()
)
}
\arguments{
\item{geometry_columns}{Character vector of geometry column names.}

\item{primary_column}{Primary geometry column name (defaults to first geometry column).}

\item{encoding}{Geometry encoding (default `"WKB"`).}

\item{geometry_types}{Optional geometry type(s). May be:
- a character vector applied to all geometry columns, or
- a named list keyed by geometry column.}

\item{crs}{CRS as PROJJSON (list) or a string convertible via \code{sf::st_crs()}.}

\item{edges}{`"planar"` or `"spherical"`.}

\item{orientation}{Optional orientation string (currently supports `"counterclockwise"`).}

\item{bbox}{Optional numeric bounding box.}

\item{covering_bbox}{Optional covering bbox spec (see GeoParquet).}

\item{version}{GeoParquet metadata version (default `"1.1.0"`).}

\item{extra}{Optional additional fields to include in the metadata list.}
}
\value{
A nested list suitable for JSON serialization via [gp_geoparquet_geo_json()].
}
\description{
Constructs a GeoParquet-compliant metadata list for one or more geometry columns.
This metadata can be serialized to JSON and stored in Parquet schema metadata
under the key `"geo"` (the GeoParquet convention).
}
