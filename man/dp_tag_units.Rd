% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{dp_tag_units}
\alias{dp_tag_units}
\title{Tag unit attributes onto columns}
\usage{
dp_tag_units(
  dt,
  spec,
  only_missing = TRUE,
  system = c("declared", "metric", "imperial"),
  unit_attr = "units"
)
}
\arguments{
\item{dt}{A `data.frame` or `data.table`.}

\item{spec}{A `dp_spec` with a `fields` table.}

\item{only_missing}{Logical. If `TRUE`, only attach units when the attribute
is currently missing.}

\item{system}{Which unit system to tag: `"declared"` (default), `"metric"`,
or `"imperial"`.}

\item{unit_attr}{Attribute name to write (default `"units"`).}
}
\value{
A list with:
- `dt`: a copied `data.table` with attributes attached
- `did_tag`: an audit `data.table` describing what was tagged
}
\description{
Attaches units as an attribute (default attribute name: `"units"`) onto
columns of an R table, based on a `dp_spec`.
}
\details{
Intended use:
- in-memory annotation to reduce accidental unit confusion during analysis
- no file I/O
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(temp = 25.1)
sp <- dp_spec_default(dt, "metric")
res <- dp_tag_units(dt, sp)
attr(res$dt$temp, "units")
}
}
