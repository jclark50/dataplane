% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{read_parquet_meta}
\alias{read_parquet_meta}
\title{Read Dataplane metadata from a Parquet file (metadata-only)}
\usage{
read_parquet_meta(
  path,
  parse = TRUE,
  preferred_prefix = "dp",
  legacy_prefixes = c("klimo")
)
}
\arguments{
\item{path}{Path to a Parquet file.}

\item{parse}{Logical. If `TRUE`, parse serialized payloads into `spec_dt`
and `audit_dt`.}

\item{preferred_prefix}{Preferred metadata prefix (default `"dp"`).}

\item{legacy_prefixes}{Optional legacy prefixes to search (default `"klimo"`).}
}
\value{
A list with:
- `prefix`: detected prefix
- `keys`: resolved key names for the detected prefix
- `kv`: raw key/value list
- `spec_dt`: parsed spec table (or `NULL`)
- `audit_dt`: parsed audit table (or `NULL`)
}
\description{
Reads schema key/value metadata from a Parquet file and (optionally) parses
the Dataplane payloads (`spec_dt`, `audit_dt`).
}
\examples{
\dontrun{
m <- read_parquet_meta("file.parquet")
names(m$kv)
}
}
