% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_utils.R
\name{filterCols}
\alias{filterCols}
\title{Filter column names by storage type}
\usage{
filterCols(dat, colType = "numeric", exlcludedatetime = TRUE, exclude = FALSE)
}
\arguments{
\item{dat}{A data.frame / data.table.}

\item{colType}{Character. Either `"numeric"` (default) or a character vector
of class names (e.g., `c("character")`).}

\item{exlcludedatetime}{Logical. If `TRUE`, columns with primary class in
`c("Date","POSIXct","POSIXt")` are excluded from the match set.}

\item{exclude}{Logical. If `TRUE`, returns columns *not* matching `colType`.}
}
\value{
Character vector of column names.
}
\description{
Returns the column names whose *primary class* matches a target set, or (if
`exclude = TRUE`) those that do not.
}
\examples{
d <- data.frame(x = 1:3, y = c("a","b","c"), t = as.POSIXct("2026-01-01") + 1:3)
filterCols(d, "numeric")
filterCols(d, "character")
filterCols(d, "numeric", exclude = TRUE)

}
