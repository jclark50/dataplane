% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{write_parquet}
\alias{write_parquet}
\title{Write a Parquet file with Dataplane metadata}
\usage{
write_parquet(
  x,
  path,
  spec = c("metric", "imperial"),
  mode = c("document", "validate"),
  scale = FALSE,
  suffix = "_i",
  drop_original = FALSE,
  tag_missing_units = FALSE,
  prefix = "dp",
  compression = "zstd",
  unit_attr = "units",
  warn_max = 8L,
  ...
)
}
\arguments{
\item{x}{A `data.frame` or `data.table`.}

\item{path}{Output `.parquet` file path.}

\item{spec}{Either a `dp_spec` or one of `"metric"`, `"imperial"` (in which case a
default spec is generated from column names).}

\item{mode}{`"document"` or `"validate"`. If `"validate"`, runs [check_units()]
(warns by default) before writing.}

\item{scale}{Logical. If `TRUE`, apply [scale_encode()] at write time.}

\item{suffix}{Suffix for storage columns (default `"_i"`).}

\item{drop_original}{Logical. If `TRUE` and `scale=TRUE`, drop base columns after encoding.}

\item{tag_missing_units}{Logical. If `TRUE`, apply [tag_units()] before writing.}

\item{prefix}{Metadata key prefix (default `"dp"`).}

\item{compression}{Parquet compression (default `"zstd"`).}

\item{unit_attr}{Unit attribute name to use (default `"units"`).}

\item{warn_max}{Passed to [check_units()].}

\item{...}{Passed to [arrow::write_parquet()].}
}
\value{
Invisibly returns a list containing `path`, `spec`, `audit`, and `kv`.
}
\description{
Writes `x` to a Parquet file and embeds Dataplane metadata in Parquet schema
key/value pairs. Optionally:
- tags unit attributes before writing
- validates unit attributes against a spec
- encodes selected numeric columns into integer storage columns
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(temp = c(25.12, 25.44), rh = c(55, 52))
sp <- spec_default(dt, "metric")
sp <- set_scale(sp, "temp", 100)
out <- tempfile(fileext = ".parquet")
write_parquet(dt, out, spec = sp, scale = TRUE)
res <- read_parquet(out, attach_units = "declared")
res$dt
}
}
