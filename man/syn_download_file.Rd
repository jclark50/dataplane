% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/klimo_synapser.R
\name{syn_download_file}
\alias{syn_download_file}
\title{Download a Synapse FileEntity to disk}
\usage{
syn_download_file(
  entity_id,
  dest_path,
  overwrite = FALSE,
  token = NULL,
  verbose = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{entity_id}{FileEntity synId.}

\item{dest_path}{Destination path (will create parent directories).}

\item{overwrite}{Logical; overwrite existing file if `TRUE`.}

\item{token}{Optional token (PAT).}

\item{verbose}{Logical.}

\item{dry_run}{Logical.}
}
\value{
`dest_path` (invisibly in dry-run, otherwise returned as a character scalar).
}
\description{
Fetches the FileEntity to obtain `dataFileHandleId`, resolves a presigned URL via
the file handle URL endpoint, and streams the file content to `dest_path`.
}
\examples{
\dontrun{
dest <- tempfile(fileext = ".csv")
syn_download_file("synFileEntityId", dest, overwrite = TRUE, verbose = TRUE)
read.csv(dest)
}
}
