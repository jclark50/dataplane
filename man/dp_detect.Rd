% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{dp_detect}
\alias{dp_detect}
\title{Detect whether a file or dataset contains Dataplane metadata}
\usage{
dp_detect(
  path,
  preferred_prefix = "dp",
  mode = c("detect", "validate"),
  scan_n = 5L,
  recursive = TRUE
)
}
\arguments{
\item{path}{Path to a Parquet file or dataset directory.}

\item{preferred_prefix}{Preferred metadata prefix (default `"dp"`).}

\item{mode}{`"detect"` or `"validate"`. `"validate"` uses a stricter threshold.}

\item{scan_n}{For dataset directories without a sidecar, number of Parquet files to sample.}

\item{recursive}{For dataset directories, whether to scan recursively for Parquet files.}
}
\value{
A list describing detection outcome (confidence, evidence keys, etc.).
}
\description{
Works on a single Parquet file or a dataset directory. For dataset directories,
this function prefers a Dataplane sidecar (e.g., `dp_meta.parquet`) and otherwise
samples Parquet files and applies a majority rule.
}
\examples{
\dontrun{
dp_detect("file.parquet")
dp_detect("dataset_dir")
}
}
