% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_read_parquet.R
\name{dp_read_parquet}
\alias{dp_read_parquet}
\title{Read a Parquet file into a data.table, optionally using Klimo metadata}
\usage{
dp_read_parquet(
  path,
  metadata = c("apply", "return", "ignore"),
  decode_scaled = TRUE,
  attach_units = TRUE,
  unit_source = c("audit", "spec"),
  system = c("metric", "imperial"),
  ...
)
}
\arguments{
\item{path}{Parquet path.}

\item{metadata}{One of:
- "ignore": do not parse metadata
- "return": parse and return metadata but do not change data
- "apply": parse and apply selected behaviors (decode scaled, attach units)}

\item{decode_scaled}{If TRUE and metadata is available, attempt to decode scaled columns.}

\item{attach_units}{If TRUE and metadata is available, attach declared unit attributes.}

\item{unit_source}{"audit" uses declared_units (most direct); "spec" uses metric/imperial fields.}

\item{system}{When unit_source="spec", choose which units column to use.}

\item{...}{Passed to arrow::read_parquet (e.g., col_select)}
}
\value{
If metadata=="ignore": data.table
Else: list(dt=..., meta=..., spec_dt=..., audit_dt=...)
}
\description{
Read a Parquet file into a data.table, optionally using Klimo metadata
}
