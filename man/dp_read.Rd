% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{dp_read}
\alias{dp_read}
\title{Read a Parquet file written with Dataplane metadata}
\usage{
dp_read(
  path,
  decode_scaled = TRUE,
  keep_storage = TRUE,
  attach_units = c("declared", "none"),
  preferred_prefix = "dp",
  legacy_prefixes = c("klimo"),
  unit_attr = "units",
  ...
)
}
\arguments{
\item{path}{Path to a Parquet file.}

\item{decode_scaled}{Logical. If `TRUE`, decode scaled columns using the audit table.}

\item{keep_storage}{Logical. If `TRUE`, keep storage columns after decoding.}

\item{attach_units}{`"declared"` or `"none"`. If `"declared"`, attach unit attributes
based on the stored spec.}

\item{preferred_prefix}{Preferred metadata prefix (default `"dp"`).}

\item{legacy_prefixes}{Optional legacy prefixes (default `"klimo"`).}

\item{unit_attr}{Unit attribute name (default `"units"`).}

\item{...}{Passed to [arrow::read_parquet()].}
}
\value{
A list with:
- `dt`: a `data.table`
- `meta`: parsed metadata from [dp_read_meta()]
}
\description{
Reads a Parquet file and returns both the data and parsed Dataplane metadata.
Optional behaviors:
- decode scaled integer storage columns back to numeric columns
- attach declared unit attributes using the stored spec
}
