% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{dp_scale_encode}
\alias{dp_scale_encode}
\title{Encode numeric columns into integer storage columns}
\usage{
dp_scale_encode(
  dt,
  spec,
  suffix = "_i",
  drop_original = FALSE,
  round_fn = c("round", "floor", "ceiling")
)
}
\arguments{
\item{dt}{A `data.frame` or `data.table`.}

\item{spec}{A `dp_spec` describing scaling intent (`writer_scale`).}

\item{suffix}{Suffix for storage columns (default `"_i"`).}

\item{drop_original}{Logical. If `TRUE`, drop the original float columns.}

\item{round_fn}{Rounding method: `"round"`, `"floor"`, or `"ceiling"`.}
}
\value{
A list with:
- `dt`: a copied `data.table` with storage columns added
- `scale_map`: a `data.table` describing what was scaled and where
}
\description{
Creates new integer storage columns (default suffix `"_i"`) by multiplying
numeric columns by their `writer_scale` in the spec.
}
\details{
Intended use:
- stable, compact integer storage while retaining predictable decimal precision
- consistent round-trip with [dp_scale_decode()]
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(temp = c(25.12, 25.44))
sp <- dp_spec_default(dt, "metric")
sp <- dp_set_scale(sp, "temp", 100)
enc <- dp_scale_encode(dt, sp)
enc$dt
}
}
