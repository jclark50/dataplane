% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{write_dataset}
\alias{write_dataset}
\title{Write an Arrow Dataset and Dataplane sidecar metadata}
\usage{
write_dataset(
  x,
  path,
  spec = c("metric", "imperial"),
  scale_document = FALSE,
  suffix = "_i",
  prefix = "dp",
  sidecar_overwrite = TRUE,
  sidecar_compression = "zstd",
  write_format = "parquet",
  partitioning = NULL,
  existing_data_behavior = NULL,
  ...
)
}
\arguments{
\item{x}{An Arrow Table, RecordBatchReader, Dataset, or object acceptable to [arrow::write_dataset()].}

\item{path}{Dataset directory path.}

\item{spec}{Either a `dp_spec` or `"metric"`/`"imperial"` to create a default spec.}

\item{scale_document}{Logical. If `TRUE`, document intended storage columns in metadata.}

\item{suffix}{Storage suffix to document (default `"_i"`).}

\item{prefix}{Metadata prefix (default `"dp"`).}

\item{sidecar_overwrite}{Logical. Overwrite sidecar if present.}

\item{sidecar_compression}{Compression used for the sidecar Parquet file.}

\item{write_format}{Dataset format (currently only `"parquet"` supported here).}

\item{partitioning}{Optional partitioning passed to [arrow::write_dataset()].}

\item{existing_data_behavior}{Optional passed to [arrow::write_dataset()].}

\item{...}{Additional args passed to [arrow::write_dataset()].}
}
\value{
Invisibly returns a list including dataset path and sidecar path.
}
\description{
Writes an Arrow Dataset (directory of Parquet files) and writes a Dataplane
sidecar metadata Parquet file (default: `dp_meta.parquet`).
}
\details{
Important: `scale_document=TRUE` **documents** intended storage columns (based on `writer_scale`)
but does not transform the dataset data. For actual scaling, use [write_parquet()] per-file
or pre-transform your table before dataset writing.
}
