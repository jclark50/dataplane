% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{set_scale}
\alias{set_scale}
\title{Set scaling intent in a spec}
\usage{
set_scale(spec, columns, writer_scale)
}
\arguments{
\item{spec}{A `dp_spec`.}

\item{columns}{Character vector of column names to edit.}

\item{writer_scale}{Numeric scalar scale factor (or `NA` to clear).}
}
\value{
Updated `dp_spec`.
}
\description{
Sets `writer_scale` for selected columns in a `dp_spec`. This does **not**
change your data by itself; it records the intent:
}
\details{
- when scaling is enabled at write time (`write_parquet(..., scale = TRUE)`),
  numeric values are multiplied by `writer_scale` and stored as integers.
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(temp = 25.12)
sp <- spec_default(dt, "metric")
sp <- set_scale(sp, "temp", 100)  # keep 2 decimals as integer storage
}
}
