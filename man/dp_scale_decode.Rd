% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{dp_scale_decode}
\alias{dp_scale_decode}
\title{Decode scaled integer columns back to numeric columns (using audit metadata)}
\usage{
dp_scale_decode(dt, audit_dt, keep_storage = TRUE, overwrite = FALSE)
}
\arguments{
\item{dt}{A `data.frame` or `data.table`.}

\item{audit_dt}{Parsed audit/plan table. Must contain:
`column`, `writer_scale`, `storage_column`, `did_scale`.}

\item{keep_storage}{Logical. If `TRUE`, keep the integer storage columns.}

\item{overwrite}{Logical. If `TRUE`, overwrite existing base columns if present.}
}
\value{
A decoded `data.table` (modified copy).
}
\description{
If a writer created columns like `temp_i` with `writer_scale = 100`, this can
recreate `temp = temp_i / 100`. Optionally keeps or drops the integer storage
columns.
}
\details{
`audit_dt` is typically obtained from metadata via [dp_read_meta()] or from
the `meta` element returned by [dp_read()].
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(temp_i = c(2512L, 2544L))
audit <- data.table(column="temp", writer_scale=100, storage_column="temp_i", did_scale=TRUE)
dp_scale_decode(dt, audit)
}
}
