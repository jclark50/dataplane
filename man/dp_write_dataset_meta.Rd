% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{dp_write_dataset_meta}
\alias{dp_write_dataset_meta}
\title{Write Dataplane dataset metadata sidecar}
\usage{
dp_write_dataset_meta(
  dataset_path,
  kv,
  prefix = "dp",
  overwrite = TRUE,
  compression = "zstd",
  sidecar_name = NULL
)
}
\arguments{
\item{dataset_path}{Dataset directory path.}

\item{kv}{Named list of key/value metadata (character scalars recommended).}

\item{prefix}{Prefix used for default sidecar name (default `"dp"`).}

\item{overwrite}{Logical. If `FALSE` and sidecar exists, stop.}

\item{compression}{Parquet compression for the sidecar (default `"zstd"`).}

\item{sidecar_name}{Optional override for sidecar file name.}
}
\value{
The sidecar path (invisibly).
}
\description{
Writes a tiny Parquet file in `dataset_path` (default file name: `dp_meta.parquet`)
containing Dataplane schema metadata in its Parquet schema key/value pairs.
}
\details{
This provides a stable, single place to read dataset-level metadata without
scanning all data files.
}
\examples{
\dontrun{
kv <- list("dp:writer"="example")
dp_write_dataset_meta("dataset_dir", kv)
}
}
