% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dp_parquet_v2.R
\name{check_units}
\alias{check_units}
\title{Check unit attributes against a spec}
\usage{
check_units(
  dt,
  spec,
  system = c("declared", "metric", "imperial"),
  unit_attr = "units",
  warn_max = 8L,
  stop_on_any = FALSE
)
}
\arguments{
\item{dt}{A `data.frame` or `data.table`.}

\item{spec}{A `dp_spec` with a `fields` table.}

\item{system}{Which unit system to validate against: `"declared"` (default),
`"metric"`, or `"imperial"`.}

\item{unit_attr}{Attribute name to read (default `"units"`).}

\item{warn_max}{Maximum number of problems to show in the warning message.}

\item{stop_on_any}{Logical. If `TRUE`, stop if any problems are found.}
}
\value{
An audit `data.table` (invisibly) describing expected vs observed units
and a status code.
}
\description{
Compares unit attributes in a table (default attribute name `"units"`) against
expected units from a `dp_spec`. By default, it **warns** and returns a report.
}
\examples{
\dontrun{
library(data.table)
dt <- data.table(temp = 25.1)
sp <- spec_default(dt, "metric")
dt2 <- tag_units(dt, sp)$dt
check_units(dt2, sp)
}
}
