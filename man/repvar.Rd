% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_utils.R
\name{repvar}
\alias{repvar}
\title{Repeat a scalar to length n (or recycle shorter vectors)}
\usage{
repvar(var, n)
}
\arguments{
\item{var}{A vector or `NULL`.}

\item{n}{Integer target length.}
}
\value{
`NULL` if `var` is `NULL`; otherwise a vector of length `n` (or the
  original vector if already length `n` or longer). If `length(var) < n`, the
  value is recycled with a warning.
}
\description{
Utility for aligning scalar parameters with vectorized data.
}
\examples{
repvar(NULL, 5)
repvar(3, 5)
repvar(1:2, 5)

}
